# Q-Discovery Agent 产品需求文档

> 项目中文名：面向科研实验批次设计的自验证量子—经典协同智能体
> 项目代号：Q-Discovery Agent
> 文档版本：v0.1
> 文档状态：立项草案
> 更新日期：2026-07-27
> 目标赛事：书生国智科探挑战赛暨飞翔杯 AI Agent/Skills 开发大赛——量子计算赛道

## Problem Statement

科研人员在优化化学反应条件、筛选候选分子或设计材料配方时，通常面对一个共同问题：候选实验很多，但真正可执行的实验预算很少。

传统做法主要依赖专家经验、网格搜索或逐个候选点的贝叶斯优化。这些方法存在以下问题：

1. 实验选择需要同时考虑预期效果、模型不确定性、候选多样性、成本、安全性和设备约束，而不是优化单一指标。
2. 实验通常以批次并行执行。单点采集函数容易把一个批次集中在相似区域，浪费实验预算。
3. 固定批次数量、预算上限、试剂冲突等组合约束会使批次选择快速变成难解的组合优化问题。
4. 普通 QAOA 原型常依赖惩罚项表达约束，容易采样出不可行解，也缺少与经典算法的公平比较。
5. 当前许多科研 Agent 能够解释论文或生成代码，但不能把自然语言科研目标稳定地转换成可检查、可执行、可复现的量子优化流程。
6. 如果系统只展示一次量子线路运行结果，用户无法判断结果是否正确，也无法知道量子模块是否真正贡献了价值。

项目首先服务于有基础数据、需要批量选择下一轮实验的计算化学或实验化学团队。首个演示场景是 Suzuki–Miyaura 偶联反应条件的批次选择；系统架构应允许后续替换为分子筛选、材料配方或其他科学实验设计任务。

核心研究问题是：

> 在相同实验预算和量子调用预算下，采用多目标建模、经典 Warm Start、固定基数约束保持和有限宽度量子邻域优化，能否比随机选择、贪心策略、经典贝叶斯优化及标准惩罚式 QAOA 更稳定地选出高价值、可行且多样的实验批次？

本项目不预设答案为“量子方法一定更好”。系统必须自动验证该假设，并如实输出有效、无显著差异或不成立的结果。

## Solution

Q-Discovery Agent 是一个可交互、可审计的科研实验批次设计系统。科研人员使用自然语言描述研究目标、候选变量、实验预算和安全约束；系统将其转换成结构化实验设计任务，用代理模型预测候选实验的效果与不确定性，再通过量子—经典协同优化选择下一批最值得执行的实验。

系统的核心目标函数同时考虑：

- 预期科研收益，例如预测产率、活性或材料性能；
- 模型不确定性，用于探索认知不足的区域；
- 批次内部多样性，避免推荐大量相似实验；
- 实验成本和预计耗时；
- 安全风险与资源约束。

对候选实验 \(i\) 定义二进制决策变量 \(x_i\)。基础批次效用为：

\[
U(x)=
\alpha \sum_i \mu_i x_i
+\beta \sum_i \sigma_i x_i
+\gamma \sum_{i<j} d_{ij}x_ix_j
-\delta \sum_i c_i x_i
-\eta \sum_i r_i x_i
\]

其中：

- \(\mu_i\) 为代理模型预测的实验效果；
- \(\sigma_i\) 为预测不确定性；
- \(d_{ij}\) 为候选实验之间的差异度；
- \(c_i\) 为成本；
- \(r_i\) 为安全或执行风险；
- \(\alpha,\beta,\gamma,\delta,\eta\) 为用户可理解、系统可校验的偏好权重。

系统至少支持“必须选择恰好 \(K\) 个实验”的固定基数约束。量子求解器优先使用保持固定 Hamming Weight 的 XY Mixer，使采样过程天然停留在可行子空间，而不是完全依赖大惩罚系数。

完整产品闭环为：

1. 用户用自然语言描述科研目标。
2. 需求解析器生成结构化任务草案。
3. 约束验证器发现缺失、冲突或不可执行的约束。
4. 代理模型为所有候选实验预测均值和不确定性。
5. QUBO 编译器构造单目标标量化问题和多个 Pareto 偏好配置。
6. 经典求解器生成基线结果与 Warm Start。
7. 量子求解器在一个或多个有限宽度候选邻域中运行 QAOA。
8. 结果验证器检查可行性、目标值、统计稳定性和基线差距。
9. 系统向用户展示推荐批次、备选批次、选择理由和验证证据。
10. 用户导入模拟或真实实验结果，系统更新代理模型并开始下一轮。

### 产品目标

1. 让不熟悉量子编程的科研人员也能完成一次可复现的量子实验设计流程。
2. 将量子算法放在实际的组合决策环节，而不是作为展示性附件。
3. 在小规模实例上提供可证明的正确性，在大规模实例上提供可量化的近似质量。
4. 每次推荐都保留从自然语言需求到最终结果的完整审计链。
5. 在比赛要求的单卡环境和时间限制内完成演示与评测。

### MVP 成功标准

#### 产品闭环

- 用户可以从自然语言目标创建一个实验设计任务。
- 系统可以输出恰好 \(K\) 个可执行实验及至少两个 Pareto 备选批次。
- 用户可以导入一轮实验结果并触发下一轮推荐。
- 每个关键阶段均产生结构化、可下载的中间产物。

#### 正确性

- 在可穷举的小规模实例上，所有正式推荐的约束满足率为 100%。
- QUBO 编译前后的目标值计算一致，数值误差不超过预先声明的容差。
- 同一数据、配置、随机种子和依赖版本能够复现核心结果。

#### 优化质量

- 在至少三个固定随机种子的回放任务中，主动学习闭环的中位 Simple Regret 优于随机批次选择。
- 在可穷举实例上，量子求解结果相对最优解的中位近似比不低于 0.90。
- 对标准惩罚式 QAOA 的比较必须采用相同量子电路调用或 Shot 预算。
- “Warm Start + XY Mixer”优于标准惩罚式 QAOA 是研究假设和消融目标，不作为允许篡改或选择性报告结果的发布门槛。

#### 性能与赛事适配

- 标准演示任务端到端运行时间不超过 10 分钟。
- 完整评测流程不超过 30 分钟。
- 默认活跃量子邻域宽度控制在 12–20 个二进制变量；具体上限根据模拟器和硬件环境标定。
- 至少准备 5 组有效的自然语言 Agent/Skill 交互样例。

## User Stories

1. As a 化学研究人员, I want 用自然语言描述待优化的反应, so that 我不需要手工编写 QUBO 或量子线路。
2. As a 化学研究人员, I want 指定配体、溶剂、碱、催化剂、温度和时间等变量, so that 系统能够理解真实的反应搜索空间。
3. As a 化学研究人员, I want 设置每轮最多可执行的实验数量, so that 推荐结果符合实验室吞吐能力。
4. As a 化学研究人员, I want 设置总成本和实验时长限制, so that 系统不会推荐无法负担的批次。
5. As a 安全负责人, I want 标记禁止组合和高风险条件, so that 推荐方案符合实验安全要求。
6. As a 科研人员, I want 在执行前查看系统解析出的结构化任务, so that 我能发现语言理解错误。
7. As a 科研人员, I want 修改系统解析出的变量或约束, so that 我能保留最终决策权。
8. As a 科研人员, I want 在约束互相冲突时得到明确解释, so that 我能修正任务，而不是得到空结果。
9. As a 科研人员, I want 导入已有实验表格, so that 系统能够利用历史数据建立代理模型。
10. As a 数据管理员, I want 查看缺失值、异常值和重复实验报告, so that 不可靠的数据不会悄悄进入优化流程。
11. As a 科研人员, I want 查看每个候选实验的预测值和不确定性, so that 我能理解系统为何推荐探索某些区域。
12. As a 科研人员, I want 在利用高预测值和探索高不确定性之间调整偏好, so that 推荐策略符合当前研究阶段。
13. As a 科研人员, I want 控制批次多样性的重要程度, so that 同一轮实验不会全部集中于相似条件。
14. As a 科研人员, I want 控制成本和安全风险权重, so that 高性能不成为唯一优化目标。
15. As a 科研人员, I want 获得多组 Pareto 备选批次, so that 我能在收益、成本和风险之间做人类判断。
16. As a 科研人员, I want 系统解释两个备选批次之间的主要差异, so that 我可以快速选择合适方案。
17. As a 量子算法研究人员, I want 查看生成的 QUBO/Ising 系数和变量映射, so that 我能审查问题建模是否正确。
18. As a 量子算法研究人员, I want 查看约束是由 Mixer、预筛选还是惩罚项处理, so that 我能判断解的可行性来源。
19. As a 量子算法研究人员, I want 使用经典松弛解或贪心解作为 Warm Start, so that 低深度 QAOA 有更合理的初始状态。
20. As a 量子算法研究人员, I want 使用保持固定基数的 XY Mixer, so that 量子采样天然选择恰好 \(K\) 个实验。
21. As a 量子算法研究人员, I want 用贝叶斯优化调整 QAOA 参数, so that 在有限量子调用预算下减少无效搜索。
22. As a 量子算法研究人员, I want 将上一轮的 QAOA 参数迁移到下一轮, so that 连续实验设计可以复用计算经验。
23. As a 量子算法研究人员, I want 限制量子子问题的最大宽度, so that 系统能够在当前模拟器或 NISQ 资源上运行。
24. As a 量子算法研究人员, I want 查看大候选池如何被拆成量子邻域, so that 我能判断分解是否引入明显偏差。
25. As a 评委, I want 看到量子模块确实改变了候选批次决策, so that 我能确认量子计算不是装饰。
26. As a 评委, I want 一键运行固定示例, so that 我能在有限时间内复现实验。
27. As a 评委, I want 查看与随机、贪心、经典 BO、模拟退火和标准 QAOA 的公平比较, so that 我能判断方案贡献。
28. As a 评委, I want 查看所有方法的计算预算, so that 更高质量结果不是通过不公平地增加资源获得。
29. As a 评委, I want 查看小规模实例的穷举最优解, so that 我能确认求解器和目标函数实现正确。
30. As a 评委, I want 查看多次随机种子的均值和方差, so that 结果不是一次幸运采样。
31. As a 评委, I want 查看消融实验, so that 我能分别判断 Warm Start、XY Mixer、参数迁移和邻域分解的贡献。
32. As a 评委, I want 查看失败案例和负结果, so that 项目的科研结论可信。
33. As a 科研人员, I want 将推荐批次导出为 CSV 或 JSON, so that 我能接入现有实验记录流程。
34. As a 科研人员, I want 导入本轮真实或模拟实验结果, so that 系统能够更新代理模型。
35. As a 科研人员, I want 查看多轮优化过程中最佳结果、后悔值和覆盖度变化, so that 我能判断研究是否正在收敛。
36. As a 科研人员, I want 暂停并恢复一个实验设计任务, so that 多轮实验可以跨天或跨设备执行。
37. As a 开发者, I want 每次运行生成唯一实验标识和配置快照, so that 日志、模型和结果可以互相追溯。
38. As a 开发者, I want 用统一接口切换本地模拟器和赛事支持的量子平台, so that 上层 Agent 不依赖单一 SDK。
39. As a 开发者, I want 求解器失败时返回结构化错误和可恢复建议, so that Agent 不会伪造成功结果。
40. As a 开发者, I want 对 Agent 可调用的工具和参数设置白名单, so that 自然语言输入不能触发任意代码执行。
41. As a 复现实验者, I want 获取依赖版本、随机种子、数据摘要和线路配置, so that 我能在另一环境重现实验。
42. As a 项目负责人, I want 自动生成算法和实验报告, so that 参赛材料与实际运行证据保持一致。
43. As a 项目负责人, I want 查看每项赛事要求对应的交付物, so that 提交时不会遗漏 skill.md、日志、脚本或演示材料。
44. As a 产品使用者, I want 在非量子模式下运行相同任务, so that 没有量子环境时仍能完成经典基线和数据检查。
45. As a 产品使用者, I want 清楚看到系统何时使用了预测、何时使用了量子采样、何时使用了确定性验证, so that 我不会把不同可信度的结论混在一起。

## Implementation Decisions

### 1. 产品边界与首个场景

- 项目作为独立应用开发，不耦合工作区中的现有口语软件或其他业务项目。
- MVP 聚焦“从有限候选池中选择下一批实验”，不直接生成无限化学空间中的新分子。
- 首个演示场景为 Suzuki–Miyaura 偶联反应条件批次选择。
- 演示使用公开且许可清晰的反应条件数据进行离线回放；正式开发前完成数据许可和字段质量审查。
- 若首选数据集不满足许可或完整性要求，可替换为公开的 Buchwald–Hartwig 或其他高通量反应优化数据，但产品接口保持不变。
- MVP 不依赖真实湿实验。历史表格中的已知结果被隐藏并作为离线“实验 Oracle”，用于模拟执行下一批实验。

### 2. 最高层测试接缝

系统的主要外部行为接缝定义为：

> 给定一份历史实验数据、候选池、自然语言目标、批次预算和固定随机种子，系统输出一个满足所有硬约束的推荐批次、可验证的目标值、经典基线比较和完整运行清单。

核心验收优先通过这一条端到端接缝完成。内部模块只在数学正确性或失败定位需要时增加更细测试。

### 3. Agent 架构

- MVP 采用一个编排 Agent 加多个确定性 Skill/工具，而不是让多个自由对话 Agent 互相生成不可控内容。
- 编排 Agent 负责理解用户意图、调用工具、解释结果和管理状态，不直接计算优化结果。
- 需求解析 Skill 将自然语言转换为版本化的 ExperimentDesignSpec。
- 数据审计 Skill 负责字段校验、缺失值、重复项、候选空间和数据泄漏检查。
- 建模 Skill 训练代理模型并输出预测均值、不确定性和模型评估。
- QUBO 编译 Skill 生成目标系数、变量映射、约束处理方式和数值范围报告。
- 求解 Skill 统一调用经典与量子求解器。
- 验证 Skill 独立重算可行性和目标值，不信任求解器自报结果。
- 报告 Skill 根据结构化运行产物生成可读报告，不允许凭空生成指标。

### 4. 工作流状态

一次实验设计运行使用以下状态：

```text
DRAFT
  → VALIDATED
  → MODELED
  → COMPILED
  → SOLVED
  → VERIFIED
  → PRESENTED
  → UPDATED
```

- 任一状态失败均进入 FAILED，并记录失败阶段、错误代码和可恢复动作。
- 只有 VERIFIED 状态的解可以成为正式推荐。
- 用户修改目标、约束、数据或权重后，所有下游产物失效并从最近的有效状态重新计算。

### 5. 核心数据契约

ExperimentDesignSpec 至少包含：

- 任务名称和科研目标；
- 候选实验唯一标识；
- 可控变量及类型；
- 待优化性能指标；
- 固定批次大小 \(K\)；
- 成本、风险和资源字段；
- 硬约束和软偏好；
- 多目标权重范围；
- 随机种子；
- 量子资源预算；
- 数据版本和特征版本。

SolutionArtifact 至少包含：

- 选中候选的唯一标识；
- 每项效用分量；
- 总目标值；
- 可行性检查结果；
- 量子与经典资源用量；
- 电路深度、Shot 数和优化迭代数；
- 求解器配置与版本；
- Pareto 配置标识；
- 随机种子；
- 基线差距；
- 验证状态。

### 6. 代理模型与不确定性

- 小数据和混合变量场景默认使用适合稀疏数据的高斯过程或树模型集成。
- 模型接口必须同时返回预测均值与不确定性；不能可靠估计不确定性的模型不得驱动探索项。
- 分类变量采用显式编码并保留原始标签映射。
- 每轮训练必须只使用当轮已经“观察”的数据，禁止从隐藏结果泄漏信息。
- 模型选择基于固定回放协议评估，而不是根据最终测试结果手工挑选。
- MVP 支持 Upper Confidence Bound 风格的均值—不确定性组合。

### 7. 多目标与 Pareto 方案

- MVP 同时支持单一标量化目标和多组权重形成的近似 Pareto 解集。
- 系统至少生成“高收益”“高探索”“低成本/低风险”三种可解释偏好方案。
- 不同量纲的目标在进入 QUBO 前进行稳定、可追溯的归一化。
- 每组权重必须保存，报告中不得只展示表现最好的一组。
- 批次多样性使用候选特征距离或领域相似度构成二次项。

### 8. 约束处理

- “恰好选择 \(K\) 个候选”为一级硬约束，优先由固定 Hamming Weight 初态和 XY Mixer 保持。
- 明确禁止的候选在编译前移除，不消耗量子变量。
- 成本上限等非固定基数约束在 MVP 中采用可验证的预筛选、拉格朗日惩罚或候选邻域构造；系统必须标记具体采用方式。
- 验证器对所有硬约束进行独立检查。任何违反硬约束的样本只能进入诊断统计，不能成为推荐。
- 惩罚系数必须由可解释规则或上界计算产生，禁止为追求结果手工调到测试集最优。

### 9. 量子—经典求解策略

- 量子求解器默认实现 Warm-start XY-QAOA。
- 经典贪心、连续松弛或经典启发式产生满足固定基数的初始候选。
- 初始量子态与 Mixer 的可行子空间保持一致。
- QAOA 角度优先采用贝叶斯优化，在有限 Shot 和电路调用预算下搜索。
- 连续主动学习轮次允许迁移上一轮参数，但必须保留“无迁移”消融。
- 小规模问题直接求解；大候选池使用有限宽度邻域分解。
- 邻域分解由经典模型选择一个活跃前沿，再将边界外变量固定，构造 12–20 变量的量子子问题。
- 邻域宽度、重启次数和量子调用预算均为显式配置，不根据最终答案偷偷增加。

### 10. 经典基线

每个正式实验至少运行：

- 随机可行批次；
- 按单点效用排序的贪心批次；
- 经典贝叶斯优化/批量采集策略；
- 模拟退火或等价 QUBO 启发式；
- 标准 X-Mixer 惩罚式 QAOA；
- 小规模实例上的穷举最优解。

比较遵循以下公平原则：

- 相同候选空间和硬约束；
- 相同历史观测数据；
- 相同实验批次预算；
- 量子方法之间使用相同 Shot 或电路调用预算；
- 报告墙钟时间、CPU/GPU 时间、量子模拟时间和调用次数；
- 预先固定随机种子集合和统计汇总方式。

### 11. 自动验证

- 验证器从候选原始数据重新计算目标值，不复用求解器内部缓存结果。
- 对小规模任务执行穷举，检查全局最优值和解的等价性。
- 对每个量子样本统计可行率、最佳值、均值、方差和有效样本数。
- 对 Pareto 解检查非支配关系，剔除被支配或重复的候选批次。
- 运行清单保存代码版本、依赖版本、数据摘要、配置、种子和硬件信息。
- 报告必须区分“算法测量结果”“代理模型预测”和“Agent 解释”。

### 12. 用户界面

MVP 提供以下页面：

1. 项目与数据导入页；
2. 自然语言目标与结构化约束确认页；
3. 候选空间和代理模型诊断页；
4. 求解配置与资源预算页；
5. 推荐批次及 Pareto 对比页；
6. 经典基线与量子验证页；
7. 多轮主动学习进展页；
8. 运行日志与产物下载页。

推荐结果的默认展示必须回答：

- 选中了哪些实验；
- 为什么选它们；
- 哪些目标存在取舍；
- 是否满足全部约束；
- 量子求解器与经典基线相比如何；
- 结果对随机种子是否敏感；
- 下一轮导入结果后会发生什么。

### 13. 技术接口

- 后端提供任务创建、数据验证、模型训练、问题编译、求解、验证、结果更新和产物导出接口。
- 长时间运行步骤采用作业接口，返回 job_id、阶段、进度、日志摘要和最终产物引用。
- 求解器使用统一 Adapter 接口，至少支持本地状态向量/采样模拟器和一个赛事推荐的量子 SDK。
- 首选量子 SDK 可从 Qiskit、PennyLane 或 UnitaryLab 中根据硬件支持和部署成本确定。
- 前后端只交换结构化数据，不通过解析自然语言日志获得关键状态。
- 所有算法参数都经过 Schema 校验和范围限制。

### 14. 可复现与交付

- 仓库提供依赖安装、构建、演示运行、完整评测和正确性检查命令。
- 项目提供符合赛事要求的 skill.md，明确自然语言输入、工具调用、输出和能力边界。
- 固定示例提供数据快照、期望结构、随机种子和参考结果区间。
- 正确性脚本独立于演示界面运行。
- 性能报告包含至少三次运行的统计结果。
- 演示材料不得使用无法由当前仓库重新生成的截图或指标。

### 15. 安全与可信边界

- Agent 只能调用白名单工具，不执行用户提供的任意代码或 Shell 命令。
- 上传数据默认本地处理；若调用外部模型或平台，界面必须明确提示数据流向。
- 不将系统输出描述为实验安全批准、化学合规意见或量子优势证明。
- 涉及危险化学品和实验条件时，系统只执行规则过滤和风险提示，最终判断由合格人员完成。

## Testing Decisions

### 测试原则

- 优先测试用户可观察行为，而不是内部实现细节。
- 最高层测试通过一次完整任务验证“输入科研目标后得到可行、可复现、带证据的推荐批次”。
- 数学模块的细粒度测试只覆盖必须严格正确的边界：目标函数、约束、变量映射、采样解码和指标计算。
- 任何带随机性的测试使用固定种子和统计容差，不断言一次采样必须得到某个固定比特串。
- 论文中的正向结论只作为研究假设，不直接写成测试期望。

### 端到端验收

1. 导入一个固定反应数据快照。
2. 输入自然语言目标、批次大小、成本上限和禁止条件。
3. 确认系统生成的结构化任务。
4. 完成代理建模、QUBO 编译、经典与量子求解。
5. 验证正式推荐包含恰好 \(K\) 个实验且满足全部硬约束。
6. 验证报告包含经典基线、资源预算、随机种子和可复现清单。
7. 导入隐藏的实验结果并完成第二轮推荐。

### 数学正确性测试

- 手工构造 4–12 个候选的小问题，与穷举最优解比较。
- 检查原始效用函数、QUBO 目标和解码后目标一致。
- 检查固定基数初态和 XY Mixer 采样始终位于目标 Hamming Weight 子空间。
- 检查变量固定和邻域缩减前后的剩余目标常数项正确。
- 检查成本、禁止组合和候选移除规则不会产生索引错位。
- 检查 Pareto 非支配排序、Hypervolume 和去重逻辑。

### 代理模型与主动学习测试

- 检查隐藏标签不会进入特征或训练数据。
- 检查不确定性输出为有限值且量纲处理一致。
- 使用固定回放协议比较随机、纯利用、纯探索和综合策略。
- 报告 Best Found Value、Simple Regret、Top-k Recall、批次多样性和模型误差。
- 使用多个随机初始批次重复测试，避免单一初始样本导致结论失真。

### 量子求解器测试

- 检查线路宽度、深度、Shot 数和调用次数均符合配置。
- 检查无噪声模拟、小量 Shot 和可选噪声模型下的统计变化。
- 在相同预算下比较随机初态、Warm Start、X Mixer 和 XY Mixer。
- 对连续轮次比较参数迁移与重新初始化。
- 对大候选池比较邻域分解前后的质量、时间、内存和量子变量数。

### Agent 测试

- 至少覆盖目标明确、信息缺失、约束冲突、数据异常和求解失败五类交互。
- 验证 Agent 不会在验证失败时宣称任务成功。
- 验证报告中的每个数值都能追溯到结构化产物。
- 验证同义自然语言可以生成等价的结构化约束。
- 验证超出能力范围的问题会得到明确边界说明。

### 性能测试

- 标准演示任务在目标环境中不超过 10 分钟。
- 完整评测不超过 30 分钟。
- 记录每个阶段耗时，定位代理建模、角度优化、量子模拟或报告生成的主要瓶颈。
- 在候选数逐步增加时记录内存、编译时间和活跃量子宽度。

### 消融实验

至少执行：

1. 移除 Warm Start；
2. XY Mixer 替换为 X Mixer + 惩罚项；
3. 移除不确定性项；
4. 移除多样性项；
5. 移除参数迁移；
6. 移除邻域分解或改用随机邻域；
7. 移除独立验证器。

消融结果必须全部报告，不只报告支持项目假设的结果。

## Out of Scope

- 不在 MVP 中控制真实机器人实验室或自动加液设备。
- 不在 MVP 中执行未经人工确认的真实化学实验。
- 不生成全新分子结构，也不解决无限化学空间中的从零分子设计。
- 不训练新的大规模化学基础模型。
- 不实现通用量子程序自动生成平台。
- 不以破解密码、金融交易或与科研无关的组合优化作为首发场景。
- 不声称实现量子加速、量子优势或超越全部经典算法。
- 不把一次最佳运行当作统计结论。
- 不支持任意用户上传 Python 代码并在服务端执行。
- 不在 MVP 中同时支持多个完全不同的科研领域；其他领域通过后续数据与领域 Adapter 扩展。
- 不以湿实验最佳产率作为首版交付门槛。

## Further Notes

### 论文到产品创新的映射

| 研究工作 | 提炼出的产品思想 | 本项目中的差异化落点 |
|---|---|---|
| Warm-starting quantum optimization | 经典松弛解初始化低深度 QAOA | 用上一轮科研数据和经典候选生成可行初态 |
| Multi-objective QAOA | 量子采样近似多目标 Pareto 前沿 | 输出科研收益、探索、多样性、成本和风险的备选批次 |
| Initial-state/Mixer alignment | 初态应与约束保持 Mixer 对齐 | 用 XY Mixer 原生保持固定实验批次数量 |
| Bayesian Optimization for QAOA | BO 减少 QAOA 参数搜索调用 | 在赛事时间和 Shot 预算内优化角度 |
| HQNS | 大 QUBO 分解为有限宽度活跃邻域 | 从数百候选中构造 12–20 变量的量子子问题 |
| QALO | 代理模型、量子优化、新数据形成闭环 | 将一次性优化提升为多轮科学主动学习 |
| QPipe | 需求、建模、代码、执行、验证分阶段留痕 | 形成可审计的 Agent/Skill 中间产物 |
| Chemma/ChemBOMAS | 化学知识和数据驱动优化结合 | LLM 负责目标与约束知识，数值模型负责预测，量子模块负责组合选择 |

### 项目创新表述

建议使用以下严谨表述：

> 本项目提出一种面向有限预算科研实验批次设计的自验证量子—经典协同 Agent。系统将多目标主动学习采集函数编译为组合优化问题，结合经典 Warm Start、固定基数约束保持 QAOA、有限宽度量子邻域和独立验证器，完成从自然语言科研目标到下一轮实验建议的可追溯闭环。

不建议在完成更系统的论文和专利查新前使用“全球首创”。当前可主张的是：在本轮检索到的工作中，尚未发现上述方法被完整组合为面向科研实验批次设计的可验证 Agent 产品。

### 核心评估指标

- Feasible Rate：满足全部硬约束的采样比例；
- Approximation Ratio：相对小规模全局最优解的质量；
- Best Found Value：固定实验预算内找到的最佳真实结果；
- Simple Regret：与数据集中已知最优结果的差距；
- Batch Diversity：同一批次候选的平均差异度；
- Pareto Hypervolume：多目标解集的覆盖质量；
- Top-k Recall：是否发现隐藏的高价值候选；
- Quantum Calls/Shots：量子资源使用量；
- Wall-clock Time：完整运行时间；
- Reproducibility Rate：固定清单下的成功复现比例。

### 建议开发阶段

#### Phase 0：数据与数学原型

- 确定公开反应数据；
- 实现离线回放 Oracle；
- 完成标量效用、QUBO 编译、穷举和经典基线；
- 用小实例验证数学等价性。

#### Phase 1：量子求解核心

- 实现标准惩罚式 QAOA；
- 实现 Warm-start XY-QAOA；
- 加入角度贝叶斯优化；
- 完成公平基线和消融实验。

#### Phase 2：规模化与主动学习

- 实现有限宽度量子邻域；
- 实现多轮代理模型更新；
- 加入多目标权重扫描和 Pareto 输出；
- 完成完整回放评测。

#### Phase 3：Agent 与产品界面

- 实现结构化需求解析和约束确认；
- 接入确定性 Skills；
- 展示推荐理由、线路资源和验证证据；
- 支持结果导入与下一轮运行。

#### Phase 4：参赛交付

- 编写 skill.md；
- 固化 5 组 Agent 交互；
- 生成正确性和性能报告；
- 完成 10 分钟演示路径；
- 准备 PPT、视频和可复现环境。

### 主要风险

| 风险 | 影响 | 应对 |
|---|---|---|
| 量子方法没有超过经典基线 | 不能宣传性能优势 | 将成果定位为可验证的量子科研工作流，并如实分析适用边界 |
| 数据集过小或存在偏差 | 主动学习结论不稳定 | 使用多个任务、多个种子和严格回放协议 |
| QAOA 模拟耗时过长 | 无法满足赛事时间 | 限制活跃宽度、调用预算和深度，缓存非关键中间产物 |
| 多目标权重被认为主观 | Pareto 结果缺乏可信度 | 展示多组偏好，不只选取最有利配置 |
| Agent 产生错误约束 | 推荐不可执行 | 强制结构化确认和独立验证，未验证结果不得发布 |
| 与近期论文过于相似 | 创新性受质疑 | 将创新放在完整闭环、约束保持、多目标科研批次和自动验证的组合，而非复刻单篇算法 |

### 文献入口

1. Egger, Mareček, Woerner, [Warm-starting quantum optimization](https://quantum-journal.org/papers/q-2021-06-17-479/), Quantum, 2021.
2. [Quantum approximate multi-objective optimization](https://www.nature.com/articles/s43588-025-00873-y), Nature Computational Science, 2025.
3. [Alignment between initial state and mixer improves QAOA performance for constrained optimization](https://www.nature.com/articles/s41534-023-00787-5), npj Quantum Information, 2023.
4. Tibaldi et al., [Bayesian Optimization for QAOA](https://arxiv.org/abs/2209.03824), 2022.
5. [Resource-Efficient Hybrid Quantum Neighborhood Selection for Large-Scale Molecular Diversity Optimization](https://arxiv.org/abs/2607.07336), 2026.
6. [Quantum annealing-assisted lattice optimization](https://www.nature.com/articles/s41524-024-01505-1), npj Computational Materials, 2024.
7. [QPipe: Leveraging LLM-Based Agentic Systems to Generate Quantum Applications for Test Optimization](https://arxiv.org/abs/2607.00939), 2026.
8. [An LLM System for Autonomous Variational Quantum Circuit Design](https://arxiv.org/abs/2606.13380), 2026.
9. [Large language models to accelerate organic chemistry synthesis](https://www.nature.com/articles/s42256-025-01066-y), Nature Machine Intelligence, 2025.
10. [ChemBOMAS: Accelerated BO in Chemistry with LLM-Enhanced Multi-Agent System](https://arxiv.org/abs/2509.08736), 2025.
